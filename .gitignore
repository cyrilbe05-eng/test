# Logs
logs
*.log
npm-debug.log*

# Dependency directories
node_modules/
dist/

# API build artifact generated during Vercel build
# (brackets escaped — unescaped they act as a glob character class and never match)
api/\[...slug\].mjs
dist-ssr/
*.local

# Environment variables — never commit secrets
.env
.env.server
.env.local
.env.*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
